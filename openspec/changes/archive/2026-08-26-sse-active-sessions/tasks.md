# Tasks: SSE 化活跃会话推送与内部事件总线

## Phase 1：DDD 分层重构 + 领域事件总线 + 全部事件生产（REST/WS 对外行为与前端 5s 轮询保持不变；唯一显式差异为同值写不再推进 `updated_at`，见 specs/task-lifecycle delta；验收依据 design D0/D2/D4）

- [ ] P1.1 domain 包落地（`internal/domain/task|session|event`）：Task 聚合根（Status/InitStatus/DeleteMode/typed Notice）；Session 独立聚合（领域 ID=`session_id`、Ownership、claim/touch/delete 规则；唯一性为领域声明 + Claim 事务保证，读到历史重复归属 fail-closed）；event 包 `Event{Topic,Type,RID,Payload}` + 闭合 Topic/Type 常量 + typed payload 构造器（Bus 不做 schema 校验）。**状态机 guard 按 design D0 的 `tasks.status` 状态机矩阵逐行实现**（普通函数/方法，如 `CanActivate`/`CanDelete`/`CanArchive`，含补偿性落账与同值 no-op 行）；init_status 状态机（`none|pending|running|succeeded|failed`）与跨字段门禁（Activate 的 init_status 五分支，以 canonical task-lifecycle spec 为准）。单测：**status 矩阵逐行表驱动**（每行断言 from/to/guard 放行或拒绝/补偿路径/是否产生迁移）；**`CanArchive`/`CanDelete` guard 覆盖 mode（Normal/Force）与 init_status 维度**（init 进行中拒绝归档/删除、Normal 拒绝 `deletion_failed` 重入、Force 放行 `deletion_failed`）；init_status 合法/非法流转表驱动；Session 构造不变量
- [ ] P1.2 结构化提交结果与 repository ports（application 层定义，非 domain 非 sqlite）：`MutationResult{Matched,Changed,UpdatedAtAdvanced}`/`TransitionResult`/`ClaimResult`/`AlignResult`；`TaskRepository`/`SessionRepository`（方法闭合为 Claim/TouchOwned/DeleteOwned/Align/OwnedSessions/OwnerOf，MUST NOT 暴露通用 Save/Upsert；Align 为显式 application 一致性操作，notice 的 expected/new 先经 Task domain 决策再由 sqlite adapter 同事务提交（仅 complete 清除分支）；**notice expected 失配 MUST 回滚整个 Align 事务并返回 typed conflict，由 application 重读 Task、重新经 domain 决策后有界重试（沿用既有 notice CAS 8 次上限语义，notice.go:459-476）**；**overflow（complete=false）的 session_overflow notice MUST 在 Align 之前经事务外 CAS（UpdateTaskNoticeCAS 结构化结果）写入：成功才调 Align；CAS 失败 MUST 返回错误且不执行 Align；Align 失败 MUST NOT 回滚该 notice（沿用 canonical opencode-orchestration spec 与 activate.go:1239-1244 现状）**）/`ProjectReader`/`EnvReader`/`CleanupDebtRepository` + `ProcessPort`/`OpenCodePort`/`WorktreePort` 接口（`ProcessPort` MUST 提供 context-aware 的会话环境读取方法——签名携带 ctx，调用方 context 取消/超时使调用在预算内终止；既有非 context 接口保留且对外行为不变，见 active-sessions-overview delta「水合调用链 context 收敛」）；`internal/infrastructure/sqlite` adapter 包装现有 store.DB；**store 同值原子 no-op 改造先行**：`UpdateTaskEnvSnapshot`/`UpdateTaskLastPort`/`UpdateTaskNotice*`（含 CAS 变体 `UpdateTaskNoticeCAS`）/`SetTaskDeleteMode`（queries.go:430-456,582-609）、align 事务内 notice 分支（queries.go:1043-1053），**以及全部 status/init_status 写方法**（`UpdateTaskStatus`/`UpdateTaskStatusConditional`/`ArchiveTask`/`RestoreTask`/`BeginDeleteIntent`（delete_mode+status+updated_at）/`CommitCreated`（status+`last_error`+init_status+updated_at）/`ClaimInitRun`/`ClaimInitRerun`/`FinishInitRun`/`ConvergeInterruptedInitRuns`），统一 SQL WHERE 排除同值（`IS`/`IS NOT` NULL 安全比较）并返回结构化 `MutationResult`——**同值口径 MUST 覆盖该语句写入的全部业务列**（`UpdateTaskStatus*` 为 status+`last_error`；init 系列为 init_status+`init_error`）；CAS 调用方按 `Matched` 收敛（现有 notice.go:196-205,228-250 的 `!replaced` 重试语义不误判同值幂等成功）；状态写方法的结构化结果 MUST 区分 status 是否实际迁移（`TransitionResult` 仅真实迁移填充 From/To，或 `MutationResult` 增列 `StatusChanged`）；`BeginDeleteIntent`/`CommitCreated` 同样返回 `TransitionResult`；`DeleteTask` 返回结构化结果 `DeleteResult{Affected, From, CascadedSessionIDs}`（受影响行数 + 被删行原状态 `from` + 事务内级联移除的剩余 session ID 集合，sqlite adapter 同一事务内先捕获再删除），供 `task.deleted{from}` 与级联 `session.deleted` 判定；初始 Create 的 `CommitCreated` 未命中后补偿写（`crud.go:138/215`）MUST 返回实际旧状态 + affected/changed；**commit helper 骨架先行**：LifecycleService 集中 commit helper（`commitTransition`/`commitTaskMutation`/`applyRuntimeChange`）在本步建立并接窄 `Publisher` 接口，初始注入 NoopPublisher（P1.6 才接真 bus），后续迁移步骤 MUST 经 helper 提交，不得散落直接写
- [x] P1.3 ServeRuntimeRegistry（`internal/application/runtime`）：一次性迁移 generation/instanceID/runtime groups/tombstone/clear 责任，单一锁域；实体身份 `RuntimeToken{instanceID,generation}`，不持有 `*Task`；迁移期间 MUST NOT 旧 Manager 与新 Registry 双写
- [x] P1.4 LifecycleService strangler 迁移，MUST 按 design D0 八步顺序逐步验收，每步保持旧 facade 单写路径且锁定 D0 不变量清单（`api.TaskBackend` 契约/DTO/OpError 映射不变、全系统单一 task keyed-lock owner、watcher/SSE callback 始终携带 generation+sessionName+instanceID（不再经 activate.go:1438-1440 无令牌入口）、**取消锁超时无锁 cleanup/CAS（activate.go:1457-1467）替换为令牌校验后的两阶段债务**、attention owner/epoch/buffer 语义保持、session claim 唯一归属/dir owned-only/overflow 基于原始列表/align 单事务、detached compensation context/runner/shutdown gate/WG join 顺序保持、Delete 静态检查先于删除意图与破坏性副作用（delete.go:67））：
  - [x] P1.4.1 冻结 HTTP/WS DTO、错误码、SQL 行为与关键副作用 trace；建立旧 Manager facade；**决策先于副作用约定落地**：每用例阶段在首个不可逆外部副作用前完成该阶段 domain 决策，guard 拒绝叶节点零副作用，依赖外部成功的事实仅在成功后推进（design D0 不变量）
  - [x] P1.4.2 引入 domain 类型、Repository ports 与结构化结果（P1.1/P1.2 产物接入）；调用路径不变
  - [x] P1.4.3 抽 `ServeRuntimeRegistry`（P1.3），一次性迁移 generation/instanceID/groups/tombstone/clear 责任；MUST NOT 双写
  - [x] P1.4.4 迁移低风险 `Get/List/Archive/Restore`，验证状态 guard、CAS 与 commit helper（NoopPublisher 阶段）
  - [x] P1.4.5 迁移 Session claim/touch/delete/align 与 Attention apply；align 保持单事务，不拆成循环调用（RunStatus 内存态为新增能力，整体在 P1.8 建立，本步不涉及）；**同步删除 legacy `AlignSessions`（queries.go:786）及其 `StoreAdapter`/`TaskStore` 接口暴露（adapters.go:228、manager.go:60）——当前已无生产调用点（activate.go:994 仅为注释引用），且其无条件推进 `updated_at` 违反全局同值原子 no-op 规则，迁移完成后 MUST NOT 保留该写路径**
  - [x] P1.4.6 迁移 Create/Retry，然后 Activate；保持创建的「前置检查 → creating → 副作用 → CommitCreated → 锁外调度」顺序（crud.go:91）
  - [x] P1.4.7 迁移 Suspend、异常 converge 与两阶段 `preCleanup/postCleanup` 债务（并发风险最高）
  - [x] P1.4.8 最后迁移 Delete、Reconcile、background、Shutdown；**同步把 `api.TaskBackend` 接口签名依赖的模型类型迁到 application/domain，api 层 import 从 legacy `internal/task` 切换到 application**；legacy facade 调用方收缩至零（随后删除或仅留别名转发层）
  - [x] P1.4.9 RuntimeToken 单字段化重构（2026-08-18 决策，design.md「instVersion 定义」为唯一契约）：`{instanceID, generation}` → `instVersion`（毫秒时间戳+随机后缀，ULID 结构手拼不引依赖）；watcher/SSE 回调签名、Registry tombstone、债务注册比对、`serve_runtime.*` 事件 RID 同步改写。独立小步：单独 fixer + 独立复跑 + oracle 复审，MUST NOT 与 P1.8 混合
- [x] P1.5 事件总线（`internal/infrastructure/eventbus`，**依赖 P1.1 的 event 子项产物**（domain event 契约由 P1.1 独占提供），其余可与 P1.1-P1.4 并行）
  - [x] P1.5.1 新建 bus 包：`Event{Topic, Type, RID, Payload}` 类型与 Topic/Type 常量、typed payload 构造器 MUST 由 P1.1 的 `internal/domain/event` 独占提供，本任务 MUST NOT 重复定义；`Topic` 闭合为 `task`/`session`/`serve_runtime`/`control`；`Type` 闭合为 `task.created`/`task.status_changed`/`task.deleted`/`task.activity_changed`/`session.claimed`/`session.touched`/`session.deleted`/`sessions.aligned`/`serve_runtime.attention_changed`/`serve_runtime.run_status_changed`/`resync.requested`；`RID` 为主体实体自己的主键（task 事件与 `sessions.aligned` 为 task 主键——会话集合持久侧无独立对象；session 单条事件为 session 主键；`serve_runtime.*` 事件为 ServeRuntime 主键 instanceID，Payload 携带 `task_id`；`resync.requested` 允许空）；各 Type 的 Payload 字段以 design 事件类型目录为唯一定义（此处不重复列举），MUST NOT 塞整表快照；`Bus.Publish`（非阻塞，订阅者缓冲满丢弃该事件并置位其溢出信号、记日志）、`Bus.Subscribe(topic)` 按**单个** topic 返回带缓冲 channel 的 `Sub`（含 `C()`/`Overflow()`/`Close()`）；多 topic 由调用方多次 Subscribe 后自行 fan-in；无第三方依赖
  - [x] P1.5.2 单测：发布/订阅/退订语义；缓冲溢出时发布不阻塞、溢出信号至少一次可见、其余订阅者不受影响；Close 后不再收到事件；Publish 与 Subscribe/Close 并发的 race 测试（`go test -race`）；未知 Type 仍投递给订阅者且不使 Publish 失败；`task.status_changed` Payload 含 from/to；`session.claimed` RID 为 session 主键且 Payload 含 `task_id`；`serve_runtime.*` RID 为 instanceID 且 Payload 含 `task_id`
- [x] P1.6 领域事件发布接入（依赖 P1.2/P1.4/P1.5；**本 lane 覆盖除 run_status 外的全部生产点——`serve_runtime.run_status_changed` 由 P1.8 建立，P1.9 才是「全部生产点」最终门槛**）：commit helper 接真 Publisher——MUST NOT 散落 Publish、MUST NOT 用 Repository decorator 隐式发布、不做 aggregate pending-events 队列；commit 结果不确定仅发 `resync.requested`（三处闭合例外见 design D2）；Publish 溢出不回滚业务提交。**生产点挂接**（按 design D2 提交点矩阵）：
  - [x] P1.6.1 生命周期生产点挂接：`CreateTask` 成功插入发 `task.created`；**全部**真实 `tasks.status` 改写发 `task.status_changed{from,to}`——创建/重试补偿（crud.go:103/115/131/138/196/209/215/399/407/411/417/426/439/460/466/471/479/491）、`CommitCreated`（crud.go:128/206/436/488）、Activate（activate.go:284/302/372）、Suspend（suspend.go:42/115/206）、Reconcile（reconcile.go:116/193/208/239/321）、Archive/Restore（crud.go:522/543）、`BeginDeleteIntent`（delete.go:93）、删除失败落账（delete.go:129/140/151/162/177/189/210/218/228/238/244/326/334、crud.go:347/352）、Retry 重入 deletion_failed→deleting（crud.go:331）、异常收敛（activate.go:1502）；同值改写（如 `creation_failed→creation_failed` 且 `last_error` 相同、`attach_shell.go:89` active→active）经同值 no-op 后 MUST NOT 发布；**status 相同但 `last_error` 不同仍是真实行变更——MUST 提交、MUST NOT 发 `task.status_changed`，按 `UpdatedAtAdvanced` 发 `task.activity_changed`**；`DeleteTask` 返回 `DeleteResult`，affected>0 时先逐个发级联移除的 `session.deleted{task_id}`（本操作内已逐项删除并发布的 session MUST NOT 重复发布；Force 路径跳过逐项删除，级联集合即全部剩余归属行）再发 `task.deleted{from}`；updated_at 推进类写入按 `Changed && UpdatedAtAdvanced` 发 `task.activity_changed`（含 `ReopenAttach` 的 active→active 写 attach_shell.go:87-90）；`ClaimInitRun`/`ClaimInitRerun`/`FinishInitRun`/`ConvergeInterruptedInitRuns` 只改 init_status，不发明领域事件——即使其写入推进 `updated_at` 也 MUST NOT 发 `task.activity_changed`
  - [x] P1.6.2 session 生产点挂接（覆盖**全部生产调用点**）：Claim——`handleSSEEvent` 与 `resolveAnchorSession`（activate.go:1354，含 ReopenAttach 路径）均须让 `ClaimTaskSession` 额外返回 changed（新插入或值实际推进），仅 changed 发布；Touch 改值变化条件（`last_seen_at < ?` + RowsAffected 判定）；Delete——`handleSSEEvent` 与 Delete 流程直接删行点（delete.go:469）均须让 `DeleteTaskSession` 返回受影响行数（affected>0 才发布）；Force 删除的级联 `session.deleted` 发布规则见 P1.6.1；`alignSessions` 聚合实际插入/时间推进/删除行数（含 `DeleteAbsentSessions` 计数），session 行计数总和 >0 才发布一次 `sessions.aligned`；align 事务内 notice/`updated_at` 真实推进另发 `task.activity_changed`（`AlignTaskSessions` 返回结构化提交结果，notice 同值原子跳过；**notice expected 失配 MUST 回滚整个 Align 事务返回 typed conflict，application 重读 Task 重决策后有界重试**）；**overflow 分支：Align 前事务外 CAS 写入 session_overflow notice 成功且真实变更（Changed）时按 updated_at 规则发 `task.activity_changed`；notice CAS 失败 MUST NOT 执行 Align、不发布；Align 失败 MUST NOT 回滚已写入 notice 与已发布事件**
  - [x] P1.6.3 attention 生产点挂接：`applyAttentionEvent` 与对账的**两个独立 accepted apply**各自按「该 apply 前后完整外部可见 Attention 快照是否变化」发布：①接管归并是独立原子 apply，归并当时 changed 则立即锁外发布，随后 REST 无论 200/404/degraded/canceled/被抢占都不得回滚或抑制；②REST 写回从归并后基线独立判断（200 替换+缓冲重放、404 清空、degraded 缓冲重放），仅相对基线再变化时再发布一次；canceled/epoch 失配不为 REST 写回再发布
  - [x] P1.6.4 异常收敛与债务生产点挂接（清理先于提交的嵌套决策表，叶节点动作唯一）：**持锁主路径**清理前捕获债务令牌与外部可见状态；第一层按 CAS 结果分叉：①committed=true → 发布 `task.status_changed{from:active,to:suspended}`、不登记；②CAS error → 保守发布可见失效后按重读分叉：②a 仍 active → 发布一次 `resync.requested` 并登记 **`postCleanup` 债务**、②b 非 active/不存在 → 仅发布一次 `resync.requested` 不登记、②c 重读 error → 发布一次 `resync.requested` 并登记 **`postCleanup` 债务**；③committed=false → 按重读分叉：③a 仍 active → 发布可见失效并登记 **`postCleanup` 债务**、③b 非 active/不存在 → 不发布不登记、③c 重读 error → 同 ②c。**锁等待超时 MUST 取消无锁 `cleanupActivationRuntime` 与无锁 CAS（activate.go:1458-1467）**：仅捕获令牌/可见快照、按需发布可见失效、发布一次 `resync.requested`、登记 **`preCleanup` 债务**。**债务两阶段**：注册项携带 generation/instanceID 令牌 + 阶段 `preCleanup|postCleanup`（mutex 保护；按 taskID 去重，新代登记原子替换）；Manager 侧维护最新 runtime 令牌 **tombstone**（创建 runtime 时更新、清理后保留，沿用 lastGen 持久代先例 manager.go:204-208/notice.go:68-75）；既有 `backgroundLoop`（manager.go:603-622）新增 tick 分支。**令牌贯穿**：serve_exit watcher/handleInfraError/SSE 入口统一携带触发 `RuntimeToken{generation, instanceID}`（watcher 不再经 activate.go:1438-1440 无令牌入口）；锁超时仅允许以触发令牌做登记前过期判定——当前 runtime 令牌匹配 → 捕获快照登记 `preCleanup`；runtime=nil 且 tombstone 匹配 → 直接登记 `postCleanup`；均不匹配 → stale 退出不登记不清理；MUST NOT 用等锁结束时刻的当前 runtime 令牌代替触发令牌。**原子登记**：过期判定+失效决策+登记 MUST 与 runtime 安装/tombstone 更新同一互斥锁域原子完成（`registerDebtIfCurrent`：锁域内按精确触发令牌重校验，通过才失效发布+登记；旧令牌不得覆盖较新令牌的既有登记；失效 apply 同样在锁域内按触发令牌再校验）。worker 取得任务锁后先按阶段分叉前置校验——`preCleanup`：①注册表令牌等于快照 ②任务仍 active ③当前 runtime 令牌等于债务令牌（runtime 允许非空）④tombstone 等于债务令牌，通过后持锁 cleanup、将同一登记原子推进为 `postCleanup` 再走 CAS；`postCleanup`：①令牌等于快照 ②仍 active ③runtime 为空 ④tombstone 等于债务令牌，通过后只重试 CAS。tombstone/令牌不匹配 → compare-and-delete 撤销，不 cleanup。**阶段并发**：同令牌重复登记单调合并取更高阶段（MUST NOT 出现 `postCleanup→preCleanup` 回退）；阶段推进为精确 CAS（taskID+令牌+当前阶段 `preCleanup` 匹配才置 `postCleanup`）；推进失败重读：同令牌已 `postCleanup` → 继续；令牌已更新 → 退出且 MUST NOT 删除；记录缺失但任务仍 active 且 runtime 已空 → 重登记 `postCleanup`。worker CAS 叶节点注册表动作：W① committed=true → 先 `task.status_changed{from:active,to:suspended}` 再 compare-and-delete；W②a CAS error+重读 active → 保留 `postCleanup`（本轮已发过 `resync.requested` 则不再重复）；W②b CAS error+重读非 active/不存在 → 先 `resync.requested` 再 compare-and-delete；W②c CAS error+重读 error → 保留 `postCleanup`；W③a committed=false+重读 active → 保留 `postCleanup`；W③b committed=false+重读非 active/不存在 → compare-and-delete、不发布；W③c 同 W②c。任务离开 active 时同步撤销登记
  - [x] P1.6.5 bus wiring：`cmd/ocdeck-server/main.go` 创建单例 bus，application 经窄 `Publisher` 接口注入（迁移期经 task facade `Options{Publisher: bus}`）；`srv.SetEventSubscriber(bus)` 须在 `RebuildRoutes()` 前调用（server.go:93-121 setter 模式）；store/sqlite adapter 不 import eventbus
  - [x] P1.6.6 生产侧单测：各提交点成功且真实变更路径产生事件；校验失败、存储错误、CAS 未命中（未命中的 CAS 提交本身不发布；其后独立补偿若真实落账仍按提交结果发布）、apply/对账无变化路径不产生**域事件**（D2 `resync.requested` 例外除外）；guard 拒绝路径断言无提交、无事件（init 进行中的 Archive/Delete、Normal 模式 `deletion_failed` 重入删除）；初始 Create 的 `CommitCreated` 未命中专项：CAS 本身无事件；补偿命中并发状态 X 且 X != `creation_failed` → 发 `task.status_changed{from:X,to:creation_failed}`；X == `creation_failed` 且 `last_error` 不同 → 提交、不发 `task.status_changed`、按跨秒规则发 `task.activity_changed`；X == `creation_failed` 全列同值或补偿未命中（行不存在）→ 不发布；store 方法 MutationResult 四类路径（同值匹配 Matched+!Changed、expected 不匹配 !Matched、同秒实变 Changed+!UpdatedAtAdvanced、跨秒实变 Changed+UpdatedAtAdvanced）；claim/touch 命中归属但值不变不发布、align 全量无变化不发布、**历史重复归属 fail-closed：sqlite 直接构造同一 `session_id` 归属两个 task 的行 → `OwnerOf` 返回 typed ambiguity/error，对应 status/diff 事件不 apply、不发布（design D0 不变量）**、attention 对账集合无变化不发布、**attention 两个独立 apply 叶节点：接管归并变化立即发布；随后 REST canceled/被新 owner 抢占不得取消该事件；REST 200/404/degraded 仅相对归并后基线再变化时再发布一次；无旧缓冲的 canceled/epoch 失配不发布**、ReopenAttach 直接 claim 后无需等待 SSE 事件即可触发事件、**锁超时不再无锁清理/无锁 CAS：登记 `preCleanup` 债务 + 按需失效 + `resync.requested`；worker 持锁后 `preCleanup` 允许 runtime 非空（令牌匹配）→ cleanup → 推进 `postCleanup` → 套用 W①/W②a-c/W③a-c（每叶写明保留或 compare-and-delete）**、updated_at 真实跨秒推进发 `task.activity_changed`（同值原子 no-op/同秒不发）；notice 相同但跨秒执行的 align 不发 `sessions.aligned`；notice-only align 发 `task.activity_changed`；align notice expected 失配 → 整个事务回滚返回 typed conflict、不产生任何事件、session 行不变；对齐期间并发新增 notice 不丢失（重读后重新决策）；有界重试后 session+notice 单事务提交成功；**overflow notice 事务外写入成功但 Align 失败 → notice 保留且已发布的 `task.activity_changed` 不回滚；overflow notice CAS 失败 → MUST NOT 执行 Align、不产生任何事件**；领域生命周期正负：CreateTask 发 `task.created`、suspended→activating 发 `task.status_changed`、activating→active 发、activating→suspended 发、active→suspending 发、suspending→suspended 发、creating→creation_failed（Create 补偿与 reconcile 收敛）发、deleting→deletion_failed 发、deletion_failed→deleting（Retry 重入）发、Archive/Restore 的 suspended↔archived 发、resume activating→active 发、resume active→active 不发 status 事件、creation_failed→creation_failed 且 last_error 相同不发、**creation_failed→creation_failed 但 last_error 不同 → 提交、不发 `task.status_changed`、跨秒发 `task.activity_changed`**、**真实状态迁移仅发 `task.status_changed`，MUST NOT 同提交另发 `task.activity_changed`（其 `updated_at` 推进由 `task.status_changed` 承载）**、converge committed 发 `task.status_changed{from:active,to:suspended}`、删除任意命中行发 `task.deleted{from}`（Force 路径先逐个发级联 `session.deleted{task_id}` 再发 `task.deleted`；Normal 路径逐项删除已发布的 session MUST NOT 重复发布）、`ClaimInitRun`/`ClaimInitRerun`/`FinishInitRun`/`ConvergeInterruptedInitRuns` 不发明领域事件——即使推进 `updated_at` 也不发 `task.activity_changed`；**令牌贯穿测试**：watcher 预校验后等锁期间持锁方换代、随后锁超时 → stale 退出不登记不清理；锁超时登记仅使用触发令牌；判定通过后登记前换代（新代已安装 runtime 并登记新债务）→ 原子 `registerDebtIfCurrent` 锁域内重校验失败，旧令牌不得覆盖新登记、不得把新代快照失效；**阶段并发测试**：同令牌重复登记阶段单调不回退（`postCleanup→preCleanup` 拒绝）、阶段推进 CAS 竞争（失败者重读：同令牌 `postCleanup` 继续/新令牌退出不删/记录缺失且 runtime 已空重登记 `postCleanup`）；**嵌套决策表七行叶节点表驱动测试**（每行断言发布事件序列/Type/精确次数/最终登记状态与阶段）：①CAS committed → `task.status_changed` ×1、不登记；②a CAS error+重读 active → 可见失效+`resync.requested`、登记 `postCleanup`；②b CAS error+重读非 active/不存在 → 可见失效+`resync.requested` ×1、不登记；②c CAS error+重读 error → 可见失效+`resync.requested`、登记 `postCleanup`；③a committed=false+重读 active → 可见失效、登记 `postCleanup`；③b committed=false+重读非 active/不存在 → 无事件、不登记；③c committed=false+重读 error → 同 ②c；**无可见字段变体：②a/②b/②c/③c 中 agentStatus/attention 均无可见失效（清理前即无可见字段）时，期望仅 `resync.requested` ×1 且按对应叶节点登记或不登记（防止实现把 resync 绑定到失效域事件存在性）**；worker 读失败保留登记；worker 读到非 active 先 `resync.requested` 再 compare-and-delete（顺序断言）；重试 committed 后先 `task.status_changed{from:active,to:suspended}` 再移除登记（成功仅发布一次）；`postCleanup` 债务要求 runtime=nil 才能重试；`preCleanup` 债务在 runtime 仍存在且令牌匹配时能持锁 cleanup 并推进阶段；旧 worker 与新代债务并发时不得 CAS/误删（compare-and-delete + tombstone 校验）；登记→Suspend→Activate→旧 tick 不得挂起新代；注册表并发 race
- [x] P1.7 外部契约实测门禁（仅阻塞 P1.8；P1.1-P1.6 与其并行）：
  - [x] P1.7.1 对运行中 opencode 实例捕获 session 状态事件真实样本，覆盖 idle/busy/retry 三类，固化为测试 fixture；确认确切 `type` 字符串与 status 字段路径（参照 archive VERIFICATION.md 与 client.go:46 的 session-status-event.ts 契约），结论记录在实现 PR 描述。捕获用 P1.7.3 同一确定性驱动步骤（busy/retry 制造与 sessionID 关联），fixture 保留事件到达毫秒时间戳
  - [x] P1.7.2 基于 fixture 写状态事件解析契约测试（type 识别、sessionID 提取、status 解析、缺字段/未知 type 忽略）
  - [x] P1.7.3 动态可靠性验证（静态 fixture 不足以判定"缺失/乱序"），按以下**预登记协议**执行（实施前写入 PR 描述，不得看到结果后调参）：确定性驱动步骤（保证可复现，P1.7.1 fixture 捕获同用）：在专用测试 worktree 创建单个测试 session 并记录其 `sessionID` 作为全程关联标识；`busy` = 向该 session 发送需数秒生成的 prompt（驱动期间轮询确认进入 busy）；`retry` = 将该测试环境 provider 指向不可达端点（本地关闭端口）或注入 429 响应后发送消息，恢复端点后回到 idle——**若 `retry` 在该环境无法稳定制造（重试状态未出现），门禁直接判失败并按 P1.7.4 转模式 B，MUST NOT 跳过该状态；** 事件流按 `properties.sessionID` 关联本 session，oracle 采样按同一 `sessionID` 过滤 REST `/session/status` 结果。记录格式：事件原始记录 JSONL（每行 `{ts_ms, type, sessionID, status}`）+ oracle 采样 JSONL（每行 `{ts_ms, sessionID, status}`），原始记录保存供复查。测试序列 = 单 session 依次 idle→busy→retry→idle 各停留 ≥3s，重复 3 轮；随后 burst 序列（改用本地可控 mock provider HTTP 桩驱动，不依赖真实模型时延：桩交替返回即时短回答（busy→idle）与 429（→retry），驱动脚本以 ≤1s 间隔连续完成 busy→retry→idle ×5 次；mock 桩端点与切换脚本命令作为协议一部分，实施前与判据一同写入 PR 描述；**驱动脚本 MUST 输出预登记 transition ledger**（每次预期转换一行 `{seq, sessionID, target_status, ts_ms}`，覆盖稳定序列与 burst 全部转换）；预登记口径：实施前写入 PR 描述的是 ledger schema、目标转换序列、每转换的匹配窗口与最短稳定时长及判据（不含运行时值）；实验运行时由脚本生成带实际 `sessionID`/`ts_ms` 的 realized ledger，随原始记录归档供复查）；随后制造一次断流重连后重放稳定序列 1 轮。对照 oracle：每次迁移停留期内以 500ms 间隔轮询 REST `/session/status`。判据：稳定序列与 burst 的完整性以**事件流 × transition ledger 逐条对照**判定——任一 ledger 转换在事件流中缺失或顺序与 ledger 不一致即失败（每个 ledger 转换预登记匹配窗口与最短稳定时长；短暂 busy 完全落在两次 oracle 采样之间不构成漏帧证据，REST oracle 仅作收敛交叉验证，不作为逐转换判据）；字段漂移；burst 迟到事件窗口锚定 ledger 最终 transition——burst 结束后 2s 观察窗内出现迟到旧事件把状态改离 ledger 最终目标值即失败；重连后 5s 内未收敛至 oracle——满足任一即门禁失败。证据输出：事件流原始记录 + oracle 采样记录 + 逐条判据结论
  - [x] P1.7.4 **门禁判定**：成功走模式 A（事件驱动）；失败切换到 spec 预定义的模式 B（后台低频探测缓存，语义见 design D4 与 stream spec），MUST NOT 临时发明第三种方案或混搭；选择结果与判据记录在 design.md 与实现 PR 描述
- [x] P1.8 agentStatus 事件驱动维护（ServeRuntime 内存态，依赖 P1.3、P1.4.5、P1.5、P1.6 与 P1.7 门禁结论）：
  - [x] P1.8.1 ServeRuntime 新增 agentStatus 内存态：session 级 `map[sessionID]SessionStatusType` + busy>retry>idle 任务级聚合 + **独立于激活代的单调 `connectionEpoch`、`connected` 标记与阶段机（`aligning → reconcilePending → valid`，且 `valid` 探测/对账失败退回 `reconcilePending`、同 epoch 重试成功回到 `valid`）**（连接建立生成新 epoch 进入 aligning；align 成功进 reconcilePending 才可对账；对账成功进 valid；断流仅使匹配 epoch 失效并终止其阶段）；所有写入收敛到唯一 apply 方法（返回聚合状态或可用性是否真实变化），断流经 `applyDisconnect(epoch)` 返回 changed 语义；**apply 在同一锁域内捕获外部投影 before/after 并返回不可变 typed delta（含 from/to/available），发布方解锁后直接用 delta 组装事件、禁止重新读取 runtime**（design D4）
  - [x] P1.8.2 `handleSSEEvent` 新增 status 事件分支（仅模式 A；模式 B 不解析不 apply）：经 `properties.sessionID` 反查归属（fail-closed，未命中忽略），经唯一 apply 方法更新内存态；所有改变 owned 成员的提交（claim——含 `resolveAnchorSession` `activate.go:1354`、delete、align 插入/删除）MUST 经同一 apply 路径维护默认 idle、重聚合与可用性；零 owned 时省略字段（空串，不是 idle）
  - [x] P1.8.3 `alignSessions` 成功后追加全量对账写入内存态（模式 A：REST `/session/status`；模式 B：进入 `reconcilePending` 后立即首次探测，此后才进入低频周期节奏——runtime 建立时仅初始化不可用，不提前探测）：**对账结果 MUST 与对齐后本任务 owned session 集合取交集**（共享目录下他任务 session 状态忽略，owned 但状态缺失按 idle）；**对账/探测成功才标记当前连接代有效；失败保持不可用、记日志、不影响生命周期**，由既有后台循环按 30s 周期持续重试（仅处理 `reconcilePending` 阶段 epoch，直至成功或该 epoch 失效；每次尝试受 context/client timeout 限制）；**对账/探测调用链全程携带调用方 ctx（context-aware tmux 环境读取 → opencode HTTP，见 active-sessions-overview delta「水合调用链 context 收敛」）**
  - [x] P1.8.4 opencode client 新增断流回调契约（`Options` 现仅 `OnReady/OnMalformed`，`client.go:230-248`）：已建立连接终止后、进入重连退避前同步调用一次；主动 context 取消 MUST NOT 触发。task 层在回调中校验 runtime 激活代 + epoch 后经 `applyDisconnect(epoch)` 标记不可用（仅 available→unavailable 时返回 changed 并发布）
  - [x] P1.8.5 新增独立快照读取方法（如 `AgentStatusSnapshot(taskID)`）：读内存快照，不可用或非 active 返回空串（omitempty 省略，降级语义不变）；**既有 `Manager.AgentStatus` 实时探测语义保持不变**（`/projects`、`/tasks/{id}` 消费者不在范围内）；模式 B 下后台循环仅周期探测 `valid`/`reconcilePending` 阶段 runtime（`aligning` 跳过），模式 A 下 valid 阶段不周期探测
  - [x] P1.8.6 单测：状态事件更新聚合值、无变化不发布、孤儿事件忽略、共享目录他任务状态被交集过滤、owned 缺失状态按 idle、deleted 清理重聚合、对账成功标记有效、首次/重连对账失败保持不可用且由后台按 30s 周期持续重试恢复（直至成功或该连接代失效，每次尝试受超时限制）、相同聚合值不重复发布、断流顺序（disconnect → unavailable → backoff → reconnect reconcile → available）与 runtime 激活代身份校验、**陈旧写回防护（对账在途 → 断流 → 陈旧成功返回不得恢复可用；断流期间后台重试成功不得恢复可用）**、**epoch 阶段门控（重连 align 在途时后台 tick 不对账/不恢复可用；align 失败后后台不得恢复该 epoch；后台重试仅处理 reconcilePending）**、**valid 期间周期探测失败退回 reconcilePending（仅模式 B；模式 A valid 阶段无周期探测）、发布一次 available→unavailable、重复失败不重复发布、同 epoch 重试成功恢复 valid**、断流时已不可用不重复发布、唯一 apply 方法并发安全（race），**并发 apply 下事件 Payload 与最终状态不串线（typed delta 锁内生成）**；**空 owned：对账成功但 owned=0 保持不可用/省略字段；claim 0→1 在 valid 下发布可用 idle；delete/align 1→0 发布 unavailable**；模式 B 分支等价测试（首次探测、30s 周期探测、探测失败置不可用、交集过滤、忽略状态事件）；`/projects` 与 `/tasks/{id}` 的 agentStatus 行为回归不变测试；**阻塞中的 tmux 环境读取在调用方 context 取消/超时后迅速返回（注入阻塞 stub 断言，不等待底层固定超时）；既有非 context 环境读取接口行为回归不变**
- [x] P1.9 Phase 1 完成门槛回归：`go build ./... && go test ./... -race` 全绿；canonical spec 行为回归（REST/WS 契约、前端轮询、状态机门禁不变）；Task/Session/ServeRuntime 三模型落位且**所有**影响事件目录的生产写点（含 P1.8 建立的 run_status 生产）经过 commit helper；启动顺序与 Shutdown 语义不变；**import-graph 验收**（`internal/api` 不再 import legacy `internal/task`；`internal/domain` 仅依赖 stdlib；infrastructure 不反向依赖 api——可用 `go list -deps` 断言或架构测试锁定）；**updated_at 语义回归**（projects DTO 字段、指挥中心排序、CAS 幂等回归，见 task-lifecycle delta 全部 Scenario）

## Phase 2：消费侧 SSE 适配（依赖 Phase 1 完成；Phase 2 不新增任何事件生产点）

- [x] P2.1 SSE 适配器消费过滤（internal/api）：指挥中心 SSE 适配器按消费过滤表标脏——`session.*`/`sessions.aligned`/`serve_runtime.*`/`resync.requested`/未知 Type/Overflow 标脏；仅 `(from==active)!=(to==active)` 的 `task.status_changed`、`from==active` 的 `task.deleted` 标脏；`task.created`、两端非 active 的 status 变更、`from!=active` 的 delete 不标脏；所有 `task.activity_changed` 标脏（无 session 时 `last_active_at` 回退 `updated_at`；有 session 时重建是多余但正确）。单测：按完整消费过滤表逐行表驱动——覆盖全部已知 Type（含 `session.touched`/`session.deleted`/`sessions.aligned`/`task.activity_changed`/`serve_runtime.*`）、未知 Type、Overflow，以及 status/delete 的正负分支（仅 `(from==active)!=(to==active)` 的 `task.status_changed` 与 `from==active` 的 delete 标脏；`task.created`/两端非 active 的 status/非 active delete 不标脏）
- [x] P2.2 抽共享快照组装 helper（`ListActiveTaskOverview` + attention + agentStatus 快照 → DTO 裸数组），REST handler 与 SSE handler 共用；`handleListActiveSessions` 实时水合（并发 8/3s 预算）移除，改读 agentStatus 内存快照；响应字段与降级行为不变
- [x] P2.3 新增 `GET /api/v1/sessions/active/stream` handler，按 design D3 建连状态机实现：对 `task`/`session`/`serve_runtime`/`control` 各 Subscribe 一次并 fan-in `C()`，四路 `Overflow()` 任一置位即视为溢出；先订阅再组装初始快照（失败四路全部退订返 500 不写 SSE 头）；仅消费过滤表命中的事件置 dirty → 写 200+headers、发送完整 snapshot 帧后 Flush（首帧立即可达；组装期间脏标记补 update）→ 事件循环（固定 500ms 合并窗口、组装失败保持脏标记重试不闭连、**任一 Overflow 置位先置 dirty=true 再窗口外重推、仅成功写出并 flush 后清 dirty**、25s 心跳）→ 客户端断开或服务 ctx 取消时四路全部退订退出；**所有帧经统一 `writeSSEFrame` 写出（Write 完整帧 + `http.NewResponseController(w).Flush()` 返回 error），任何 Write/Flush 错误立即退订退出**
- [x] P2.4 服务关停链路：`http.Server.BaseContext` 设为服务进程 context；关停先取消 stream 再 Shutdown
- [x] P2.5 改造 `statusRecorder`（`internal/api/server.go`）：增加 `Unwrap()` 与 **`FlushError() error`**（委托 `http.NewResponseController(r.ResponseWriter).Flush()`；ResponseController 优先匹配 `FlushError`，仅实现无返回值 `Flush()` 会吞底层 flush 错误，可保留 `Flush()` 兼容包装），保证 SSE handler 经中间件栈可 flush 且错误可传播
- [x] P2.6 Go 侧测试：首帧快照（裸数组）且无需等心跳立即可读、订阅先于组装的竞态（组装期间变更补 update）、初始组装失败返 500、事件触发 update、窗口合并、组装失败保持连接并重试、**Overflow 置位 → 组装失败 → dirty 保持 → 心跳 tick 重试成功**、心跳帧、401 拒绝、断连/服务 ctx 取消后订阅归零且 Shutdown 预算内退出、推送路径无 opencode 调用与无写副作用断言、statusRecorder 200 透传 + **FlushError 委托与底层 flush 错误传播（注入底层 FlushError 失败）**、snapshot/update/overflow/heartbeat 各自写失败即退订退出；**对外 `event:` 仅 snapshot/update（心跳为 `: ping`）；领域 Type 从不作为 SSE event 名、Payload 不外发；`task.created`（repo/dir 双路径）与两端非 active 的 `task.status_changed`（含跨秒迁移——生产侧已保证其不伴随 `task.activity_changed`）不产生 update；`session.claimed`/`session.touched`/`session.deleted`/`sessions.aligned`/`serve_runtime.attention_changed`/`serve_runtime.run_status_changed`/`resync.requested`/`task.activity_changed`/未知 Type 与跨越 active 边界的 `task.status_changed` 产生同构裸数组 update；数组元素字段与 REST `activeSessionDTO` 对齐（含 `attention.permissions`/`questions` 子结构，`agentStatus` omitempty）**
- [x] P2.7 前端订阅（web/src，可与 P2.1-P2.6 并行）：
  - [x] P2.7.1 新增 fetch streaming SSE 客户端：Bearer 头、ReadableStream 分帧解析（`event:`/`data:`/注释行，处理跨 chunk 粘包；snapshot/update 同一解析路径裸数组）、401 → clearToken + UNAUTHORIZED_EVENT 且停止重连、指数退避重连（1s 起步 ×2、上限 30s、首个有效帧到达后重置）；`close()` 置永久 closed 标志 + 中断当前 fetch + 取消在途退避计时器
  - [x] P2.7.2 `CommandCenterPage` 移除 `usePoll(pollSessions, 5000)`，改为 mount 订阅/unmount close；帧驱动 `setSnap`；保留 initialized/attempted/error/lastSuccessAt 并**修改 `resolveSessionsBootstrap` 判定**：整页 loading 仅在 projects 自身未初始化时进入；sessions 未首帧一律为独立「连接中」（抑制全局与分区空态），消除现有 `projectsInit=true 且 projectsLen=0 且 sessions 未 attempted → loading` 分支（CommandCenterPage.tsx:109-110；既有测试 command-center.test.ts:510-513 同步改写）；首次连接错误走 error 且不与连接中并存；双快照 join 不变；**删除/改写页面"每 5 秒刷新"的用户可见文案（CommandCenterPage.tsx:257），改为连接状态中性表述**
  - [x] P2.7.3 前端测试：分帧解析（跨 chunk 粘包；注释行与未知 event 忽略；非法 JSON/非数组 data → onError + 保留旧数据 + 终止当前连接退避重连且不重置退避）、401 停止重连、断线退避重连且首个有效帧重置退避、**退避等待期间 close 后不再发起 fetch**、卸载 AbortController 清理关闭连接、订阅异常保留旧数据、**projects 已有数据 + sessions 首帧未到 → 渲染 projects-only 且展示连接中、不整页 loading、不真空态**、**projects 已成功初始化但为空 + sessions 首帧未到 → 展示连接中，不整页 loading、不真空态**、**projects 非空 + sessions 首帧为 `[]` → 继续渲染 projects-only 数据、不显示全局空态**、**两侧均成功初始化且 join 后三区均无任务 → 显示全局空态**、首次连接错误展示错误且不与连接中并存、页面不再对 `/api/v1/sessions/active` 发起定时轮询的断言、**不再出现固定 5 秒刷新提示文案**
- [x] P2.8 端到端验证（依赖全部 Phase 2 lane）：
  - [x] P2.8.1 手工/脚本联调：启动服务端，建立 SSE 连接收到首帧 snapshot；触发任务 session 活动后在 500ms 合并窗口到期后收到 update（允许明确的组装/flush 调度容差）；杀连接观察前端退避重连；关停服务观察连接及时释放
  - [x] P2.8.2 `go build ./... && go test ./... -race` 与前端 `tsc`/测试全绿
- [x] P2.9 驱动包归拢 `internal/infrastructure/`（2026-08-26 决定，本迭代内完成）：opencode/process/pty/worktree/git/ai/lifecycle/store 八包 `git mv` 至 `internal/infrastructure/`（包名不变，仅 import 路径经 ast_grep_replace 批量重写）；`config/` 保留顶层（组装根配置，非端口背后驱动）；`task/` facade 不动（收缩是独立事项）。更新 import-graph 架构断言测试路径；零行为变化，单一 commit
