# Tasks: git-page-enhancements

来源：design.md「实现阶段划分」（1 infra → 2 task+API → 3 前端 → 4 滚动协同，阶段 4 可与 2/3 并行）；行为契约见 specs/git-operations delta；精确签名、错误矩阵与测试矩阵见 design.md D1–D9 与 Wiring/Test Plan。

## 1. infrastructure git 原语

- [x] 1.1 在 `internal/infrastructure/git/` 新增 `func MergeBase(ctx context.Context, dir, baseOID, headOID string) (string, error)`（执行 `git merge-base`；仅 exit 1 经 `gitExitCode` 判定产生 typed `ErrNoMergeBase` sentinel，其他非零 exit 透传 stderr）与 `func ResolveHeadOID(ctx context.Context, dir string) (string, error)`（执行 `git rev-parse --verify --quiet --end-of-options HEAD`；exit 1 无输出 → typed `ErrUnbornHead`，exit 0 → OID，exit 128 等致命失败透传 stderr）。验证：单元测试覆盖 exit 0/1/128 三分支（可用 /tmp 下真实临时 git 仓库：有共同祖先/无共同祖先/unborn HEAD/损坏仓库），全部通过。
- [x] 1.2 在 `internal/infrastructure/git/types.go` 新增 `BranchDiffFile{ Path string; Additions int; Deletions int; IsBinary bool }`，并新增 `func BranchDiffFiles(ctx context.Context, dir, mbOID, headOID string) ([]BranchDiffFile, error)`：执行 `git diff --numstat -z <mbOID> <headOID>`，复用 `parseNumstatZ`；二进制 `-` 计数按既有口径标记 IsBinary 且不计行；结果 MUST 按路径 UTF-8 byte-wise 字典序排序后返回（MUST NOT 直接遍历 parseNumstatZ 返回的 map）；条目数超 `MaxStatusFiles` 返回 `ErrTooManyFilesChanged`；空结果（mb==HEAD）返回空列表而非错误。验证：单元测试覆盖 numstat 解析、byte-wise 排序（含非 ASCII 路径）、二进制条目、空结果、超限，全部通过。
- [x] 1.3 验证：在 `internal/infrastructure/git` 包运行 `go test ./internal/infrastructure/git/...` 全绿，且 `go vet` 无告警。

- [x] 1.4 修复 `parseNumstatZ`（`internal/infrastructure/git/parser_numstat.go`）对 git 2.55 rename 记录形态的解析缺陷：实测 rename 记录为 `<add>\t<del>\t\0<old>\0<new>\0`（stat 头带尾随 tab 后接 NUL 分隔 old/new），现 parser 按「stat 头无第三个字段」假设将头记录解析为空 path 跳过、old/new 记录因无 tab 被跳过 → rename 条目整体丢失（BranchDiffFiles 列表缺 rename 文件；Status 侧 applyRenameNumstat byRename/byPath 全 miss，rename 行数静默为 0）。修复要求：rename 条目以新路径产出（与 D4「rename 按列表新路径判定」一致）；Status 侧 rename 行数恢复真实统计且 MUST NOT 出现 byRename+byPath[newPath] 双计；既有 Status/numstat 测试不回归。验证：rename fixture 测试（BranchDiffFiles 列表含新路径条目且统计正确；Status rename 行数正确无双计）+ `go test ./internal/infrastructure/git/...` 全绿。

## 2. task 层 + API + wiring（依赖 1）

- [x] 2.1 在 `internal/application/dto.go` 新增 `GitBranchDiffFilesDTO{ BaseRef string \`json:"baseRef"\`; Files []GitBranchDiffFileEntry \`json:"files"\` }` 与 `GitBranchDiffFileEntry{ Path string \`json:"path"\`; Additions int \`json:"additions"\`; Deletions int \`json:"deletions"\`; IsBinary bool \`json:"isBinary"\` }`（json tag 风格与既有 GitFileDTO 一致；MUST NOT 含任何 OID 字段）；GitDiffDTO 八字段不改。验证：`go build ./internal/application` 通过。
- [x] 2.2 在 `internal/task/gitops.go` 新增包级私有 helper `func resolveBranchDiffRefs(ctx context.Context, worktree, base string) (baseOID, headOID, mergeBaseOID string, err error)`：仅接收 base（path 非输入）；依次 `ResolveRefOID(base)` → `git.ResolveHeadOID` → `git.MergeBase(baseOID, headOID)`。错误映射：base 解析失败 → invalid_input（其后 MUST NOT 执行 ResolveHeadOID/MergeBase/BranchDiffFiles/blob 读）；ErrUnbornHead → invalid_state；ErrNoMergeBase → invalid_state；其他 exit 非零 → git_error 透传 stderr。验证：task 层单测按子命令断言调用计数（base 非法时零后续 git 调用）；非 sentinel 致命失败传播测试——`ResolveHeadOID` 致命失败（exit 128 类）与 `MergeBase` 其他非零 exit 均映射 git_error 且透传 stderr，失败后 MUST NOT 继续后续 git 调用。
- [x] 2.3 在 `internal/task/gitops.go` 新增 Manager 方法 `GitBranchDiffFiles(ctx context.Context, taskID, base string) (application.GitBranchDiffFilesDTO, error)`：base 词法校验（空 → invalid_input，零 git 调用）→ `tryLockTask`（conflict）→ GetTask（not_found）→ worktree_path 空（invalid_state）→ `assertGitRepoTask`（project 不存在 not_found；非法 kind/mode internal；dir invalid_input）→ helper → `git.BranchDiffFiles` → task 层逐字段映射 `git.BranchDiffFile` → `application.GitBranchDiffFileEntry`（infra MUST NOT 依赖 application DTO）；`BaseRef` 字段 MUST 写入用户输入的原始 `base` 值（MUST NOT 为空或被规范化改写）；ErrTooManyFilesChanged 与 numstat 执行失败 → git_error 透传（既有 status 口径 gitops.go:84-86）；只读、不进 repo 写锁。验证：task 层错误矩阵测试（任务不存在/worktree 空/project 不存在/dir/任务忙/base 空/base 非法/无共同祖先/unborn/超限）全绿；另有 dirty worktree/index fixture 的真实临时仓库测试，断言分支列表不混入 staged/unstaged/untracked 变更。
- [x] 2.4 在 `internal/task/gitops.go` 新增 Manager 方法 `GitBranchDiffFile(ctx context.Context, taskID, base, path string) (application.GitDiffDTO, error)`：base 非空校验 + path 经 `git.ValidateDiffPath` 校验（均先于 helper，失败 invalid_input 零 git 读）→ 与 2.3 相同门禁链 → helper → `BranchDiffFiles` 归属校验（path 不在变更集合 → invalid_input 且零 blob 读；rename 按列表新路径判定；归属校验失败 MUST NOT 执行 blob 读）→ 旧侧 `ReadRefSideContent(ctx, dir, mergeBaseOID, path)` 先读（失败 git_error 透传且 MUST NOT 读新侧）→ 新侧 `ReadRefSideContent(ctx, dir, headOID, path)` → 两侧 `normalizeDiffSideContent` → 按既有规则组装 GitDiffDTO（isBinary 清空两侧、truncated 或运算）；rename 条目两侧均按新路径读取，MUST NOT 传递 oldPath。验证：task 层测试覆盖归属拒绝零 blob 读、旧侧失败零新侧读、纯新增/纯删除存在性、rename 新路径语义，全绿。
- [x] 2.5 在 `internal/api/tasks.go` `TaskBackend` 接口新增 `GitBranchDiffFiles` / `GitBranchDiffFile` 两方法签名（与 2.3/2.4 精确一致）；在 `internal/api/git.go` `registerGitRoutes` 注册 `GET /api/v1/tasks/{id}/git/branch-diff/files` 与 `GET /api/v1/tasks/{id}/git/branch-diff/file`，handler 命名 `handleGitBranchDiffFiles` / `handleGitBranchDiffFile`，错误映射复用既有 opError 路径。验证：`go build ./...` 通过（task.Manager 自动满足扩展后接口，组合根无新 wiring）。
- [x] 2.6 更新测试 fixture：`internal/api/p3_review4_fixes_test.go:191-201` `fakeTaskBackend` 增加两个 branch-diff 方法的 no-op 默认实现；`internal/api/git_api_test.go:14-59` `mockGitBackend` 以 statusFn/diffFn 同款注入模式覆盖为可注入函数；新增 API 测试覆盖路由接通 + 各错误码 → HTTP 映射（沿用既有断言风格），且 files 响应 MUST 断言精确 JSON key 集（存在 `baseRef`、`files`，不存在任何 OID 字段）**且成功响应的 `baseRef` 值精确等于请求的原始 `base`（传入文本 ref 断言相等）**。验证：`go test ./internal/api/...` 全绿。
- [x] 2.7 验证：`go test ./internal/...` 全绿；`go vet ./...` 无告警。
- [x] 2.8 local-path MODIFIED requirement 验收（spec「repo 项目 local-path 模式任务的 git 能力」）：既有门禁链对本 change 的增量为——新 branch 两端点经 2.3/2.4 复用 `assertGitRepoTask`，local-path 天然放行、dir 天然拒绝，无需新增门禁代码；本任务补齐验收：task/API 层测试断言 repo+local-path 任务可调用 `GitBranchDiffFiles`/`GitBranchDiffFile`（worktree 落点为项目目录当前 checkout）、dir 任务调用返回 invalid_input；并回归既有 local-path 行为不被本 change 破坏——status/diff/commit/push/编辑读写放行且落点为项目目录（worktree_path 即项目路径）、**diff review 放行（`DiffSourcePortAdapter.ReadLocked` 同一门禁，review 落点为项目目录当前 checkout，来源 (ref, path, untracked) 由 UI 传入）**、push 为 `git push -u origin <当前分支>` 且非 force、detached HEAD 时 status 分支留空不阻断且 push 失败透传（gitops.go:74 / worktree.go:236 现状语义）、UI 入口（Git tab 对 local-path 可见、工作台页头不展示 task.branch、任务行分支维持隐藏、**GitPanel 内展示实时分支 status.branch**）。若回归发现既有行为缺测试，随本任务补齐。验证：相关 go 测试全绿，GitPanel status.branch 展示由 5.2 的 local-path smoke 覆盖。

## 3. 前端文件列表与分支视图（依赖 2 的契约；UI 结构可与 2 部分并行）

- [x] 3.1 `web/src/types.ts` 新增 `GitBranchDiffFileEntry{ path: string; additions: number; deletions: number; isBinary: boolean }` 与 `GitBranchDiffFilesResult{ baseRef: string; files: GitBranchDiffFileEntry[] }`（无 OID 字段）；`web/src/api.ts` 新增 `gitBranchDiffFiles(taskID, base)` 与 `gitBranchDiffFile(taskID, base, path)`（精确签名见 design Wiring/Test Plan frontend 行）。验证：`pnpm --dir web typecheck`（或等价 tsc）通过。
- [x] 3.2 GitPanel 混合列表（D5）：以「展示条目」替代 `groupFiles`——每个 GitFileDTO 展开为 1-2 条 `{ path, status, ref, untracked }`（staged→ref='HEAD'；unstaged&&!untracked→ref=''；untracked→untracked=true；staged+unstaged 同路径两条），**条目 MUST 保留源 GitFileDTO 的 additions/deletions/isBinary 用于既有统计与二进制标记展示（不丢字段）**；排序 UTF-8 byte-wise（MUST NOT localeCompare），同路径 staged 先于 unstaged；文件名颜色按状态区分并附 title/aria-label 非颜色状态文本（颜色映射经 6.1 初裁、6.7/6.9 两次裁决更新为最终值：staged 成功绿 var(--success) / unstaged teal rgb(128,203,196) / untracked 琥珀 var(--warn)）；commit 勾选维持路径级 `Set<string>`（同路径双条目共享勾选）；点击条目打开 diff 来源语义不变。验证：前端单测覆盖三色映射、byte-wise 排序（含非 ASCII）、同路径双条目、勾选共享，以及**三种状态与同路径双条目的增删行数、二进制标记展示断言**，全绿。
- [x] 3.3 列表横向滚动（D7）：`.git-files` 容器内增加 `min-width: max-content` 内容层，行 `min-width: 100%; width: max-content`；移除 `.git-file-path` 的 `direction:rtl` 省略号技巧，改正常 LTR 全路径无 ellipsis（legacy-components.css:1191-1201）；不影响 `.git-side` 外侧拖拽把手。验证：手动或测试确认长路径可横向滚动查看完整路径+统计，纵向滚动与条目点击正常。
- [x] 3.4 tree 形态（D6）：前端按路径分段投影；目录节点按目录路径唯一、可折叠/展开、仅渲染含变更文件的目录；文件叶子 key = `path + "\0" + 后缀`（uncommitted 后缀 staged|unstaged|untracked，branch 固定字面量 `branch`，MUST NOT 新增第四状态枚举）；同级按路径段 UTF-8 byte-wise 排序，同路径多条目顺序与 flat 一致：已提交 → 已暂存 → 未暂存 → 未跟踪；flat/tree 显式切换控件，默认 flat，会话内保留不持久化；目录折叠状态会话级；两形态下颜色/统计/点击行为一致。验证：前端单测覆盖 tree 投影、key 唯一性（同路径双叶子）、折叠、排序、形态切换，全绿。
- [x] 3.5 视图切换与 baseRef 接线（D8 上半）：GitPanel 新增视图状态 `'uncommitted' | 'branch'`（默认 uncommitted，会话内保留）与 `baseRef` prop（现有 props 仅 taskID/active/agentBusy，GitPanel.tsx:47-56）；TaskWorkbenchPage.tsx:762-768 渲染处传入 `task?.base_ref ?? ''`；base 输入 draft/applied 分离，失焦或 Enter 生效（输入过程零请求），生效前 trim，trim 后空值回落任务 base_ref 重载；任务无 base_ref 且生效值为空时：applied 置空、generation 递增、清空 branch 列表/选中/diff/error/stale、显示「请输入基础 ref」、零 API 请求、在途旧响应丢弃；**任意 applied baseRef 变化（合法 A→合法 B、回落默认值、或置空）MUST 执行同一完整状态转移**：generation 递增、清空 branch 选中文件与 diff、在途旧响应全部失效丢弃、按新 applied 值重新拉取列表（applied 为空时零请求）；manual 标记首次手改置位，baseRef prop 后续变化仅在 !manual 时更新。验证：前端单测覆盖默认值填入、空值留白、生效时机、空值回落、无 base_ref 零请求、prop 不覆盖手工值，以及 **A→B 变更时在途旧列表/旧文件响应不得写入新 base 状态**（新 base 使用新 generation），全绿。
- [x] 3.6 branch 状态生命周期（D8 下半）：branch active diff 身份 `baseRef + generation + path`；组件级全局单调递增 `requestSeq` + `latestListSeq`/`latestFileSeq` 游标；列表/文件请求捕获 (baseRef, generation, seq[, path])；响应写入条件 = generation 一致且 seq 为该类最新（文件另需 path==当前选中；**经 6.5 补强：已提交文件响应另需当前选中为已提交条目且 baseRef+generation+path 全匹配**），不满足一律丢弃（成功/失败同样判定）且 MUST NOT 改变当前错误状态/重载标志；失败（满足写入条件）→ 列表 stale+手动重载 / diff stale+手动重试，MUST NOT 自动重试（点击才分配新序号并请求）；列表重载后已选路径消失 → 清空选中不报错；branch 全部状态操作 MUST NOT 触碰 uncommitted 视图状态；generation/requestSeq MUST NOT 序列化进 query/body/response。验证：前端单测覆盖同代乱序（快速切换文件 A→B、连续刷新列表）旧响应不覆盖、stale 失败不覆盖当前状态、失败手动重试、两视图状态隔离、generation 不进入请求参数，全绿。
- [x] 3.7 只读门控与视图动作（D8）：**门控语义经 6.4 更新为按条目类型判定**——已提交条目只读（不传 annotations/批注回调/editIO/编辑资格刷新/离开守卫），未提交条目完整能力（与 uncommitted 视图逐字一致）；branch 视图渲染 ReviewPanel 与 commit 勾选/提交输入区（仅作用于未提交条目）；视图切换离开事务：切换前执行当前编辑会话的现有 leave guard（挂在未提交条目 DiffViewer 上），拒绝则停留；动作表经 6.5 更新：branch 视图 refresh = 重拉已提交列表+选中已提交条目 diff+既有 loadStatus（未提交状态两视图共享）；push 成功两视图均执行既有 loadStatus。验证：前端单测覆盖按条目类型门控、leave guard 拒绝停留、refresh/push 视图分发，全绿。
- [x] 3.8 验证：`pnpm --dir web test` 与 `pnpm --dir web build`（或 typecheck）全绿。

## 4. DiffViewer 横向滚动协同（D1；可与 2/3 并行）

- [x] 4.1 在 `DiffViewer.tsx:862` 编辑器创建 effect 内、`new MergeView(...)` 之后安装滚动协同：组件级 `pendingRef = { a: Set<number>, b: Set<number> }`；`mergeView.a.scrollDOM` / `mergeView.b.scrollDOM` 各挂 `{ passive: true }` scroll 监听；`sync(from, to)` = 值相等短路 + clamp（`clamp(from.scrollLeft, 0, to.scrollWidth - to.clientWidth)`）+ 实际位移时将写入值入 to 侧 pending；handler 先判启用态（side-by-side 且未换行且 `!singleSidedRef.current` 且 merge 渲染态，先于 membership 判定），禁用态清空两侧 pending 并 return；membership 命中 → echo 消费（clear+return，MUST NOT 反向改写）；未命中 → 用户事件（clear + sync）。MUST NOT 用布尔 guard / domEventHandlers / rAF；MUST NOT 触碰纵向 scrollTop；监听中 MUST NOT 调 syncInlineHostWidth。验证：见 4.3 测试。
- [x] 4.2 生命周期双清理路径：创建 effect cleanup 先移除两个监听、清空 pendingRef 两侧 Set，再 `view.destroy()`；独立「启用谓词 effect」依赖渲染布尔值（mode/wrap/singleSided/merge 态），谓词变 false 时渲染提交后无条件清空两侧 pending（不依赖 scroll 事件、不触碰 editor/监听安装；存在性不进重建 deps）。验证：见 4.3 测试。
- [x] 4.3 测试：沿用 `web/src/__tests__/diff-split-ratio.test.tsx` DOM 检查风格（jsdom 无布局，`Object.defineProperty` 注入 scrollLeft + 派发合成 scroll 事件）——必测：两侧最大可滚动距离不同时 clamp 写入目标侧且其 echo 不回拉源侧；交错序列「A→B 程序写入 → B 在 echo 前被用户拖动 → B 旧 echo 迟到」源侧不回拉、最终收敛；双侧→单侧→双侧（无重建、单侧期间零滚动事件）恢复后首次滚动不消费旧 pending；同值例外自愈（pending 含当前偏移时同值用户事件允许本次不同步，下一事件另一侧立即追上且无持续循环）；快速交替拖动最终收敛无振荡；换行/unified/单侧坍缩不启用。验证：新测试全绿，且对基线实现（无协同）失败（有效性证据）。
- [x] 4.4 验证：`pnpm --dir web test` 全绿。

## 5. 集成回归

- [x] 5.1 全仓自动化验证：`go test ./...`、`go vet ./...`、`pnpm --dir web test`、`pnpm --dir web build` 全部通过（`TestAllocatePort_ExcludeSkipsLastPortAndScan` 为已知环境性豁免）；既有 git 面板行为测试无回归。
- [x] 5.2 浏览器实测 smoke（人工实现 review 现场验证）：local-path 任务 Git tab 可用；D7 文件列表长路径横向滚动（flat/tree、窄面板）且纵向滚动与拖宽把手无回归；并排 diff 不换行时双 pane 横向滚动协同。（人工 review 通过，含配色三次裁决与 tree 布局二次裁决的现场确认）

## 6. 视觉与分支视图混合调整（人工实现 review 反馈，用户已裁决）

- [x] 6.1 状态颜色重映射（用户裁决值）：staged → 偏绿 teal `rgb(128,203,196)`、unstaged → 琥珀色系（`--warn` 色阶取色，具体色值由 designer 定）、untracked → 偏红 `rgb(247,118,106)`；title/aria-label 状态文本不变。验证：前端单测更新三色断言并全绿。（配色后经用户二次裁决更新，最终值见 6.7）
- [x] 6.2 条目布局（用户裁决）：flat 与 tree 形态的文件条目均先展示文件名、随后展示目录路径（次要视觉层级）；tree 文件节点 MUST NOT 仅展示 basename。验证：前端单测断言 flat/tree 条目渲染文件名+目录路径，全绿。（tree 形态布局后经用户二次裁决改为仅文件名，见 6.8）
- [x] 6.3 分支视图混合单列表（用户裁决，spec「分支对比变更文件列表」已改）：branch 视图列表 = 已提交条目（branch-diff）+ 未提交条目（status 按 D5 展开）按路径 UTF-8 byte-wise 统一排序合并；同路径已提交+未提交条目独立不去重，**同路径多条目顺序固定为：已提交 → 已暂存 → 未暂存 → 未跟踪**（flat 与 tree 共用）；已提交条目视觉可区分（颜色由 designer 定）、无暂存状态、只读。验证：前端单测覆盖混合排序（含同路径已提交+staged+unstaged+untracked 四条目，flat 与 tree 两种形态均断言固定次序「已提交 → 已暂存 → 未暂存 → 未跟踪」）、已提交条目视觉区分，全绿。
- [x] 6.4 branch 视图按条目类型门控（用户裁决，替代原整体只读）：已提交条目 DiffViewer 不传 annotations/批注回调/editIO/编辑资格刷新/离开守卫（只读）；未提交条目传齐完整能力（与 uncommitted 视图逐字一致，批注三元组不变）；branch 视图渲染 ReviewPanel 与 commit 勾选/提交输入区（仅作用于未提交条目）；未提交相关状态（status/勾选/选中/diff/编辑会话）两视图共享，视图切换与 base 变更均不清空；leave guard 挂在未提交条目 DiffViewer 上，视图切换前仍执行。验证：前端单测覆盖已提交条目只读、未提交条目完整能力、共享状态跨视图保留、leave guard 拒绝停留，全绿。
- [x] 6.5 视图动作表更新：branch 视图 refresh = 重拉已提交列表（listSeq 递增）+ 选中已提交条目的 diff（fileSeq 递增）+ 既有 loadStatus（未提交共享状态）；push 成功两视图均执行既有 loadStatus；已提交列表/文件失败与序号模型契约不变。**已提交文件响应写入条件补强（design D8 已改）**：另需当前选中条目为已提交条目且其 `baseRef + generation + path` 与请求捕获值全匹配（仅比 path 不足以防同路径已提交/未提交条目交错选择竞态）。验证：前端单测覆盖 refresh/push 分发 + 同路径已提交/未提交交错选择时旧已提交响应被丢弃，全绿。
- [x] 6.6 验证：`pnpm --dir web test` 与 `pnpm --dir web build` 全绿；`openspec validate git-page-enhancements --strict --no-interactive` 通过。 更新 tasks.md 勾选状态与实现对齐；运行 `openspec validate git-page-enhancements --strict --no-interactive` 通过。
- [x] 6.7 状态颜色二次裁决（用户裁决，覆盖 6.1 的 staged/unstaged 取值）：unstaged → teal `rgb(128,203,196)`（原 staged 色让位）、staged → 成功绿 `var(--success)`（亮 `oklch(0.62 0.15 155)` / 暗 `oklch(0.72 0.15 155)`，随主题自适应）、untracked 偏红与已提交 muted+badge 不变；token 定义于 `web/src/design-system.css`（`--git-staged/--git-unstaged/--git-untracked`）。验证：前端单测三色断言更新并全绿。（untracked 配色后经用户三次裁决改为琥珀，见 6.9）
- [x] 6.8 tree 形态条目布局二次裁决（用户裁决，覆盖 6.2 的 tree 部分）：tree 文件节点仅展示文件名（目录层级由树结构表达，文件名后 MUST NOT 跟目录路径）；flat 形态维持「文件名+目录路径」不变；两形态 `title`/`aria-label` 均保留完整路径。验证：前端单测断言 flat 条目渲染文件名+目录路径、tree 文件节点仅文件名且目录路径文本不出现，全绿。
- [x] 6.9 untracked 颜色三次裁决（用户裁决，覆盖 6.1 的 untracked 取值）：untracked → 琥珀 `var(--warn)`（复用设计系统警告色 token，随主题自适应）；staged 成功绿、unstaged teal、已提交 muted+badge 不变。验证：前端单测三色断言更新并全绿。
