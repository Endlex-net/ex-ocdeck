# Tasks: fix-serve-connection-churn

## 1. 共享 loopback Transport

- [x] 1.1 `internal/opencode/client.go`：新增包级共享 transport（`http.DefaultTransport.(*http.Transport).Clone()` 后置 `Proxy = nil`），`NewClient` 的 `httpClient` 与 `sseClient` 均绑定它；保留各 Client 独立 `http.Client` 与超时语义；同步修正现有关于 `ProxyFromEnvironment` 不豁免 127.0.0.1 的过时注释（Go ≥1.16 已豁免 loopback，`Proxy: nil` 是显式不变量）
- [x] 1.2 `internal/opencode/client_test.go`：新增测试——起真实 listener，两个不同 `NewClient` 实例对同一 host:port 各发一次请求，经 ConnState/DialContext 计数断言仅建立 1 条 TCP 连接（真实复用证据，非仅断言指针相同）；多 host 场景（≥2 个 host:port 各两轮访问）断言每 host 仅 1 条连接；断言 `httpClient` 与 `sseClient` 绑定同一共享 transport；断言共享 transport 的 `Proxy == nil`、`MaxIdleConns > 0`、`IdleConnTimeout > 0`（有界回收不变量）

## 2. 端口轮换重试闭环

- [x] 2.1 `internal/task/activate.go`：`allocatePort` 增加 `exclude int` 参数（lastPort 快路径与轮转扫描均跳过；0 表示不排除），更新全部调用点（首次分配与 delete.go 等其他调用点传 0，行为不变）
- [x] 2.2 `internal/task/activate.go`：`waitServeReadyOrDead` 进程死亡分支包装哨兵错误（如 `errServeSessionDied`），使调用方可用 `errors.Is` 区分进程死亡与健康超时
- [x] 2.3 `internal/task/activate.go`：`startServeWithPortRetry` 重试闭环——(a) wait 失败后先查 `ctx.Err()`，非 nil 直接返回（MUST NOT kill/allocate/persist）；(b) 进程死亡（`errors.Is(err, errServeSessionDied)`）跳过 KillSession 直接进预算检查（无 disposition、无 notice）；(c) 健康超时执行 KillSession 门禁（notice 本地写用 `context.WithoutCancel` + 有界 ctx（预算 10s）；写成功 → 普通终态错误；写失败 → 终态并返回包裹 `pendingCleanupError` 的错误，载荷 = {sessionName, CleanupTickets, reason, retryable, noticeErr}，实现 `Unwrap`，MUST NOT allocate/persist/NewSession）：err != nil → 记 retryable kill_failed notice 后终态；clean → 继续；reap_failed → 记 notice 后终态（active 不携带 retryable debt）；snapshot_failed/kill_failed → 记 notice 后终态；snapshot_missing_degraded → 记 notice 后继续（写失败 → 终态 + pending handoff，MUST NOT 继续轮换）；未知/矛盾 KillResult（含 default 分支与 disposition/SessionKilled 一致性校验）→ 按共享 fail-closed 分类 helper 显式记 kill_failed notice（分类逻辑收敛为共享 helper，轮换路径 switch 与 cleanup 路径 collect 变体共用；cleanup 路径 MUST NOT 再经 `recordResidualNoticeFromDisposition` 的 `dispositionToNotice` 静默忽略未知值，notice.go:255-260）后终态；(d) 最后一次尝试失败 → 终态，MUST NOT 再 allocate/persist；(e) `allocatePort(lastPort=旧端口, exclude=旧端口)`，失败时 MUST 包装 aerr（修正现 activate.go:587 误包健康检查 err）；(f) `persistEnvSnapshot` 失败 → 终态，MUST NOT NewSession（保留现 activate.go:596-597 边界）；(g) 轮换路径终态错误聚合 wait 错误 + kill/disposition + notice 错误全部可得上下文（Probe 终态返回探测错误本身，外层 compensation 结果仅聚合进 last_error、不改变返回错误）；(h) 任何 Probe 失败（含 ctx 取消、ErrServeNotReady、unauthorized、能力不兼容、未知）→ 直接返回错误，MUST NOT 本地 kill、MUST NOT 轮换，kill + notice 委托外层 `runActivateFailureCompensation`（修正现 activate.go:607 丢弃 KillResult 丢 tickets）；(i) 类型契约：`pendingCleanup`{sessionName, cleanupTickets, reason, retryable} + `pendingCleanupError`{pendingCleanup, noticeErr, cause}（`Unwrap`→cause）；(j) `cleanupActivationRuntime` 既有签名（返回 error）保留；新增 collect 变体返回 []cleanupObservation（**每个 cleanup notice intent 一条**：KillSession 非 clean disposition 及 HasSession/KillSession infra 错误直记 kill_failed 均形成 observation——{sessionName, reason, retryable, cleanupTickets, persisted}；clean 不形成 observation、不覆盖/取消既有 pending；notice 写失败 persisted=false，写成功 persisted=true 仍须收集参与归并；KillResult 分类用共享 fail-closed helper）供 Activate 失败路径使用；其余三个既有调用点（锁超时 best-effort 收敛 activate.go:1139、运行时主动收敛 activate.go:1164、reconcile resume 失败清理 reconcile.go:167）继续调用原函数，行为不变（保持现状首写聚合语义，不做 pending 回放）；(k) `runActivateFailureCompensation` 回放：cleanup 结束后、最终 DB 收敛前，fold 输入为按发生顺序的 cause pendings（`errors.As(cause)` 检出）+ cleanup observations，同会话归并为单条——reason/retryable 以**最新 observation** 为准（后续写成功且 disposition 改变时不得被旧 pending 覆盖回旧值）、cleanupTickets 为全部 persisted=false observation 的 union；仅当同会话存在 persisted=false observation 时产生回放项；回放阶段使用**单个**新的 detached 有界 ctx（整个阶段一次预算，非逐项；超时复用 `activateCompensationFinalizeTimeout`=10s；MUST NOT 复用 cleanup compCtx），回放项按首次出现顺序确定性遍历、每项恰好一次额外 `recordResidualNotice`，回放错误独立于首次写错误聚合进 last_error（回放仍失败仅存 last_error，store 持续不可写为不可约边界）
- [x] 2.4 `internal/task/` 测试（与 spec scenarios 一一对应）：健康检查持续失败时相邻两次 NewSession 使用不同端口（仅要求相邻不同，A→B→A 合法）；最后一次失败后无 allocate/persist（NewSession/kill 次数精确核对）；进程已死亡时 KillSession 调用次数为 0 且成功轮换、无 notice；KillSession err != nil 时终态、记 kill_failed notice、分支后增量为零（首次 NewSession 与初始 env 快照属分支前基线，不计入"零"）；reap_failed 时记含 tickets 的 notice 且终态（不继续激活）；reap_failed 已杀会话 + 首次 notice 写失败时：返回错误携带 `pendingCleanupError`，外层补偿 `errors.As` 检出后无需二次 kill（会话已不存在）、重试 `recordResidualNotice` 成功且 tickets 最终完整落 notice；二次写失败时 last_error 聚合含 notice 错误；snapshot_failed/kill_failed 时记 notice 且终态零新会话；snapshot_missing_degraded 时记不可重试 notice 且继续轮换；snapshot_missing_degraded 首次 notice 写失败时终态、不继续轮换、经 pending handoff 由外层重试持久化；未知 disposition 与矛盾 KillResult 均显式按 kill_failed 记 notice 且终态（断言未经 `recordResidualNoticeFromDisposition` 的静默不记录路径）；persistEnvSnapshot 失败时终态、不得创建下一次 serve 会话（NewSession 总次数保持为已发生的尝试次数，典型场景为 1）；notice 写失败（注入 store/CAS 失败）时终态、零 allocate/persist/NewSession；终态错误断言聚合 wait + kill/disposition + notice 上下文（非仅 health check timeout）；单端口范围（Min==Max 且被排除）明确返回 exhausted 分配错误；端口分配失败时错误包含分配错误；ctx 取消路径重试函数零本地 kill/allocate/persist（直接测 `startServeWithPortRetry`），完整 Activate 路径断言外层 compensation 执行清理 kill 并收尾；Probe 全部分类（ErrServeNotReady、unauthorized、能力不兼容、未知错误）均不本地 kill、不创建下一端口会话，外层 compensation 执行 kill：clean 时断言无 notice，注入 reap_failed/kill_failed 时断言 tickets/notice 不丢失，注入外层补偿首次 notice 写失败后恢复时断言 pending 载荷统一重试成功且 tickets 落 notice；Probe 返回错误不可变（负向合同）：完整 Activate 路径注入外层 kill/notice/回放各种结果时，断言 Activate 返回错误保持原 Probe 分类与文案、不含 cleanup/disposition/notice 上下文（外层结果仅出现在 last_error）；回放顺序与归并：同会话旧 kill_failed（含 tickets）+ 新 snapshot_missing_degraded 两条原始 pending 归并后仅回放一次，断言最终 notice 仅一条、tickets 完整 union、reason/retryable 取新值（snapshot_missing_degraded）；该唯一回放失败时断言错误进入 last_error；更新 observation 不覆盖（scenario「外层清理获得更新终态时回放不覆盖」）：轮换路径 kill_failed notice 写失败形成 pending，外层第二次 kill 同会话得 snapshot_missing_degraded 且写成功时，断言回放后 reason/retryable 为 snapshot_missing_degraded（不被旧 pending 覆盖回 kill_failed）且 tickets 完整含首次写失败的 tickets；其余 cleanup 调用点行为不变：至少一个非 Activate 调用点（converge 锁超时/主动收敛或 reconcile resume 失败清理）notice 写失败时仍按现状首写聚合、无 pending 回放；回放 ctx 预算：cleanup compCtx 已耗尽时回放仍用新 detached 有界 ctx 成功执行（单个阶段 ctx，非逐项 N×超时）；本地 notice 写 ctx 预算：KillSession 期间请求 ctx 被取消后，断言本地 notice 写使用 detached 且带 deadline 的 ctx（预算 10s，非已取消的请求 ctx）；UI 展示/提示为主 spec 继承基线，本 change 无 UI 验收任务；clean 不覆盖决策：本地 kill_failed notice 写失败形成 pending、外层第二次 kill 返回 clean 时，断言旧 pending 仍按原 kill_failed reason 回放且 tickets 完整落库；外层 infra error + notice 写失败：HasSession infra error 与 KillSession infra error 两种直记 kill_failed 路径在写失败时均形成 observation 并经归并回放落库；Probe 补偿路径注入未知/矛盾 KillResult 时断言经共享 fail-closed helper 记 kill_failed notice（不静默忽略）；任务最终 suspended + last_error

## 3. 验证

- [x] 3.1 验证（真实验证范围，既有 `web/embed.go: pattern dist` 缺失致 `go build ./...` 与 `TestWatchdog_KillsServerOnParentDeath` 失败，与本 change 无关，已豁免）：
  - 通过：`go build ./internal/opencode/ ./internal/task/ ./internal/process/`
  - 通过：`go test ./internal/opencode/ ./internal/task/ -count=1`
  - 通过：`go vet ./internal/task/`
- [x] 3.2 新增测试有效性证据：在旧实现（未共享 transport / 未排除旧端口 / 未加 kill 门禁与 ctx 短路）下运行新增测试确认变红，新实现下变绿（基线对照或等价 mutation 验证）
